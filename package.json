{
  "name": "resume-world",
  "version": "0.1.0",
  "private": true,
  "description": "A data-driven game engine plus swappable content pack: the software itself is the resume.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "vitest run --coverage",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "build:content": "tsc -p tsconfig.content.json",
    "validate:content": "npm run build:content && node scripts/validate-content.mjs",
    "check:literals": "node scripts/check-career-literals.mjs",
    "check:host-coupling": "node scripts/check-host-coupling.mjs",
    "check": "npm run build && npm run test && npm run lint && npm run format && npm run validate:content && npm run check:literals && npm run check:host-coupling",
    "test:fast": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.6.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0",
    "vite": "^8.1.3",
    "vitest": "^4.1.10"
  }
}
