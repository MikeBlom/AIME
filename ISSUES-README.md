# Resume.World — Issue Plan & Bootstrap

This is the operating model for turning the `/docs` specs into executable GitHub issues that Claude Code (Fable) builds one PR at a time. It explains the structure, how to create the issues, and how work flows.

## What gets created

- **5 milestones** — one per phase (0–4).
- **5 epic issues** — one per phase, each a tracking checklist of its children with exit criteria.
- **40 build issues** — the executable units. Each is one Claude Code session and one PR.
- **Labels** — `type:*`, `phase:*`, `area:*`, `size:*`, `status:ready|blocked`, `parallel-safe`, `gate:human`, `question`.

Everything is generated by `bootstrap_issues.py`. The `/docs` specs are the durable source of truth; each issue is a thin wrapper that references spec requirement IDs (e.g. `FR-ARCH-021`) rather than duplicating them.

## Why this shape (not one giant doc, not 40 flat async issues)

A single mega-doc blows Fable's context, produces an un-reviewable PR, and has no checkpoints. Forty fully-parallel issues hammer a codebase whose architecture is still forming, causing constant conflicts and incompatible interface assumptions. The answer is a **phased dependency graph**: a sequential spine for the foundation, then parallel fan-out for leaf systems.

## The phases

| Phase | Milestone | Issues | Execution |
|-------|-----------|--------|-----------|
| 0 | Foundations | 10 | **Strictly sequential.** Repo, CI, engine Core, content loader, walking skeleton. Each merges before the next starts. All human-gated. |
| 1 | Core Systems | 9 | Parallel across disjoint areas. Rendering, input, camera, movement, physics, animation, audio, UI, save/load. |
| 2 | World Systems | 9 | Parallel. Quest, dialogue, NPC, world sim, day/night+weather, buildings, inventory, achievements, mini-games host. |
| 3 | Content & Polish | 7 | Parallel. Mini-game catalog, full reference pack, art, accessibility, localization, analytics, performance. |
| 4 | Delivery | 5 | Mostly parallel; launch-readiness is the sequential closer. |

A phase does not begin until the previous phase's epic is closed.

## How the dependency graph works

Every build issue lists `Depends on: #N`. An issue is **ready** when all its dependencies are closed; the generator labels it `status:ready` (no deps) or `status:blocked` (has deps). Fable always takes the lowest-numbered `status:ready` issue in the earliest open phase. After a merge, any dependent whose dependencies are now all closed flips to `status:ready`.

The generator validates the graph before creating anything and refuses to run if there is a cycle or a dangling dependency. Current status: **40 issues, no cycles, no dangling deps.**

## The self-PR loop (per issue)

Fable follows the build loop in `CLAUDE.md`:

1. Claim the issue, read the referenced `/docs`.
2. Branch `feat/<n>-<slug>`.
3. Implement only the Deliverables and Interface Contract.
4. Run the full CI gate locally (build, tests, lint, schema validation, "no career literals" check).
5. Open a PR with `Closes #<n>` and an acceptance-criteria checklist.
6. Self-review against the acceptance criteria; run a security-review on boundary/input/content/persistence issues.
7. **Merge policy:** issues labeled `gate:human` (all of Phase 0 plus a few sensitive later ones) wait for your approval; everything else auto-merges when CI is green and self-review is clean.
8. Unblock dependents.

CI is the safety net that makes leaf auto-merge acceptable, so the `ci-quality-gate` issue (Phase 0, #2) is a hard prerequisite for trusting the automation.

## How to create the issues

Prerequisites: the [`gh` CLI](https://cli.github.com/) installed and authenticated (`gh auth status`), and a GitHub repo to target.

```bash
# 1. Preview the plan (no network, no gh needed):
python3 bootstrap_issues.py --print-plan

# 2. Preview every gh command without creating anything:
python3 bootstrap_issues.py --dry-run

# 3. Create for real, from inside your repo clone:
cd /path/to/your/resume-world-repo
python3 /path/to/bootstrap_issues.py

# ...or target an explicit repo:
python3 bootstrap_issues.py --repo your-org/resume-world
```

The script is two-pass: it creates all issues first, records each slug's real issue number, then rewrites bodies so `Depends on:` references and the epic checklists point at actual issue numbers.

Notes:
- Run against an **empty issue tracker** for clean numbering. Milestones/labels are created idempotently (`--force`); issues are not deduped, so don't run the create step twice against the same repo.
- Recommended repo settings before turning Fable loose: branch protection on `main` requiring the CI checks, and squash-merge enabled.

## Suggested repo layout for the issues + specs

```
your-repo/
  CLAUDE.md                      # operating protocol (from this bundle)
  docs/
    00-README.md 01-Vision.md 02-System-Architecture.md 03-Data-Model-and-Content-Pipeline.md
  scripts/
    bootstrap_issues.py          # this generator
  ISSUES-README.md               # this file
```

The specs for systems 10–49 don't exist yet by design: each Phase 1–4 issue lists "author `docs/NN-*.md`" as a deliverable, so Fable writes the detailed system spec (following the skeleton in `docs/00-README.md`) as the first step of building that system. That keeps the spec and the code in the same PR.

## Adjusting the plan

Edit the `ISSUES` / `EPICS` lists in `bootstrap_issues.py`. Add an issue by appending a dict with a unique `slug` and its `deps`. Re-run `--print-plan` to confirm the graph still validates. To split an issue that grows too large mid-build, file the split as new issues rather than expanding the original.

---

**Version 1.0 — Mike Blom.**
