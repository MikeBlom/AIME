/**
 * Platform Adapter (bottom layer) — the only layer that touches the host
 * environment: render surface, input devices, audio, timers, storage, and
 * networking. Exposes narrow interfaces so the rest of the engine is
 * host-agnostic. See docs/02-System-Architecture.md, "Architectural Layers".
 *
 * Interfaces arrive with the platform-adapter issue; this placeholder only
 * anchors the layer's location and import path.
 */
export const LAYER = 'platform';
